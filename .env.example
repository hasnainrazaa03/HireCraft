
# Optional. A folder of résumé PDFs on your machine, mounted read-only so the
# browser extension can offer them alongside the ones uploaded to HireCraft.
# Expects base/*.pdf and applications/<Company>/*.pdf beneath it.
# RESUMES_DIR=/Users/you/Desktop/Resumes
