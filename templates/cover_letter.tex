((# ===================================================================
    HireCraft cover letter template.

    Uses the same LaTeX-safe Jinja delimiters as base_resume.tex; all values
    are escaped automatically by the renderer's finalize hook.
   =================================================================== #))
\documentclass[11pt,letterpaper]{article}

\usepackage[top=1in, bottom=1in, left=1.1in, right=1.1in]{geometry}
\usepackage{xcolor}
\usepackage{hyperref}
\usepackage{parskip}

\definecolor{accent}{HTML}{1A3E6F}
\definecolor{muted}{HTML}{444444}

\hypersetup{
  colorlinks=true,
  urlcolor=accent,
  pdftitle={((( basics.name ))) -- Cover Letter},
  pdfauthor={((( basics.name )))},
  pdfcreator={HireCraft}
}

\newcommand{\cvsep}{\hspace{5pt}\textperiodcentered\hspace{5pt}}

\pagestyle{empty}
\setlength{\parindent}{0pt}
\setlength{\parskip}{0.85em}
\raggedright

((# A letter that runs onto a second page reads as a letter nobody edited, and
    the reader has usually stopped by then. The renderer escalates `density`
    until it fits: margins first, then paragraph spacing, then leading, and the
    type size only at the last step — shrinking the text is the change a reader
    notices, so it is the one made last. #))
((* if density|default(0) >= 1 *))
\geometry{top=0.8in, bottom=0.7in, left=1in, right=1in}
\setlength{\parskip}{0.7em}
((* endif *))
((* if density|default(0) >= 2 *))
\geometry{top=0.65in, bottom=0.6in, left=0.9in, right=0.9in}
\setlength{\parskip}{0.55em}
\linespread{0.98}
((* endif *))
((* if density|default(0) >= 3 *))
\geometry{top=0.55in, bottom=0.5in, left=0.85in, right=0.85in}
\setlength{\parskip}{0.45em}
\linespread{0.96}
\AtBeginDocument{\fontsize{10.5pt}{12.6pt}\selectfont}
((* endif *))

\begin{document}

((# ------------------------------ LETTERHEAD ------------------------------ #))
\begin{center}
  {\LARGE\bfseries ((( basics.name )))}\\[5pt]
  {\small
    ((* if basics.location *))((( basics.location )))\cvsep((* endif *))%
    ((* if basics.phone *))((( basics.phone )))\cvsep((* endif *))%
    \href{mailto:((( basics.email | url )))}{((( basics.email )))}%
    ((* if basics.linkedin *))\cvsep\href{((( basics.linkedin | url )))}{((( basics.linkedin | display_url )))}((* endif *))%
  }
\end{center}
\vspace{0.6em}
{\color{accent}\rule{\linewidth}{0.6pt}}
\vspace{0.4em}

((* if date_line *))
((( date_line )))
((* endif *))

((* if company *))
((* if hiring_manager *))
((( hiring_manager )))\\
((* endif *))
((( company )))
((* endif *))

((* if hiring_manager *))
Dear ((( hiring_manager ))),
((* else *))
Dear Hiring Team((* if company *)) at ((( company )))((* endif *)),
((* endif *))

((* for paragraph in paragraphs *))
((( paragraph )))

((* endfor *))

\vspace{0.6em}
Sincerely,\\[0.9em]
((( basics.name )))

\end{document}
